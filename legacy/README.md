# Legacy surfaces

这里保存 manual-first 迁移前的自动化工作流材料。它们不属于默认入口，也不会被 GitHub Actions 自动触发。

当前归档内容包括旧的 PR workflow。旧 CLI、task/role 状态机、watcher、release engine、tests 和 evidence 仍在原路径保留，待人工试运行完成后再按施工计划分批删除或迁移。

归档内容只用于兼容读取、历史核对和迁移诊断，不应出现在新的执行代理 prompt 中。
