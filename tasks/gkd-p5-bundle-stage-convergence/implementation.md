# P5 Implementation

## Internal Design

Extend the trusted main facade to derive project-stage arguments and expose one deterministic transition while preserving low-level diagnostic commands.

## Execution Details

Add focused contracts for automatic derivation, idempotency, drift refusal, and skill input reduction. Keep all generated machine facts path-minimized.
