# GKD-P1 Implementation

## Internal Design

The executor will add a separate production transaction model rather than widening the temporary-home helper. The model must have a declarative managed inventory, staged desired state, a durable recovery record and explicit terminal validation. The two legacy role filenames are managed removal targets, while arbitrary global AGENTS policy remains outside this portable transaction and is handed to P2. Exact internal names and file placement remain implementation details provided they preserve the documented public contract and existing fail-closed boundaries.

## Execution Details

Implement only in the registered worktree. Add focused contracts for legacy role deletion and legacy-role absence after doctor, regenerate canonical bundle metadata after source changes, run the repository-approved fixed-base verifier, commit the delivery document before the machine delivery transition, push the full fixed head and stop for trusted-main acceptance.
