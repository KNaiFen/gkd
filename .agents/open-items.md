# Open Items

已批准并待实施的任务见根目录 `plan.md`：

- 验证项目级 `gkd_execute`、`gkd_ci_monitor`、`gkd_accept` 预设能被当前运行时以 `agent_type` 发现和调用。
- 扩展 main 路由，新增需求问答、项目适配和 CI 优化 Skills。
- 新增只读、可复用的 GitHub 长流程监控脚本，并完成四条路径的端到端验收。

写入型自动执行仍须用户明确选择；所有 GitHub 写操作须另行授权。
