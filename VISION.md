# GKD Vision

## 使命

让 Agent 编码工作保持由用户控制，并通过可读的计划、Git worktree 和进度报告在不同 session 之间继续，同时让主代理能够直接审查执行结果。

## 服务对象与用户承诺

GKD 服务于需要把复杂编码工作交给 Agent、又不愿失去决定权和可审查性的用户。GKD 主动调查现状、发现约束并给出有依据的推荐；材料性方案、自动化程度和外部动作由用户决定。系统对授权边界、失败事实和剩余风险保持诚实，不把愿景、便利或自动化推断成新的许可。

## 成功标准

- 用户能看清工作目标、工作目录、行为约束和当前进度。
- 新 session 能通过计划、报告和 Git worktree 继续工作，不依赖旧对话线程。
- 主代理可以直接查看 diff 和报告，决定通过或返工。
- 记录以人类可读为主，机器格式和自动化只在确有需要时使用。
- 默认流程保持短小；旧发行物和历史证据可以只读保留，但不为它们提供迁移入口，也不增加普通任务成本。

## 核心原则

- 用户控制优先。GKD 可以主动调查和推荐，但不能替用户作出材料性决定，也不能借执行便利扩大授权。
- 计划先于执行。主代理先写清目标、目录和约束，再让执行代理开始工作。
- 报告服务于交接。执行代理持续记录判断和下一步，报告不伪装成机器状态。
- 主代理拥有审查决定。通过和返工由主代理根据 diff、计划和报告作出。
- Git worktree 是代码事实源；普通任务不要求额外运行时、锁或证据数据库。
- 流程成本保持最少必要。增加步骤必须直接帮助理解、交接或审查。
- GKD 不扩张为通用安全、自动运维或无人值守发布平台。

## 冲突时的决策顺序

发生冲突时，依次服从：用户意图与数据保护；代码正确性；计划中的范围和约束；可读交接与主代理审查；流程简洁。后项不得覆盖前项。

## 明确非目标

- 不替代用户作出材料性产品或外部动作决定。
- 不把 GKD 建成通用安全运营、威胁检测或 Cyber 平台。
- 不把 automatic route、fixed-head acceptance 或机器合同作为普通任务前置。
- 不要求普通任务承担旧发行物、历史 watcher 或发布矩阵的成本。
- 不用更多流程、更多上下文或更多并发本身衡量成功。

## 演进规则

愿景只在长期使命、承诺或取舍顺序发生材料性变化时更新，并须先获得明确决定。具体实现、短期任务、操作步骤和易变事实留在各自权威层。演进应保留向后可理解的理由和迁移路径；任何新增机制都要说明它如何改善用户控制、正确性、交接或最少必要流程。
