# GKD Vision

## 使命

让长时间 Agent 编码工作始终可由用户控制，能在中断后恢复，以固定事实验证，并可在不同项目与环境间移植，同时保护资源受限的本机。

## 服务对象与用户承诺

GKD 服务于需要把复杂编码工作交给 Agent、又不愿失去决定权和可审查性的用户。GKD 主动调查现状、发现约束并给出有依据的推荐；材料性方案、自动化程度和外部动作由用户决定。系统对授权边界、失败事实和剩余风险保持诚实，不把愿景、便利或自动化推断成新的许可。

## 成功标准

- 用户能看清当前事实、已批准范围、下一步动作与停止条件。
- 长任务可持久交接、可靠恢复，并在固定事实不漂移的前提下继续。
- 开放式判断由 Agent 完成，脆弱状态与重复动作由确定性脚本生成和校验。
- 证据可复现、可比较，并绑定明确的代码、配置、外部状态与执行结论。
- GKD 发行物能自我验证；消费项目只承担与其接入范围相称的验证成本。

## 核心原则

- 用户控制优先。GKD 可以主动调查和推荐，但不能替用户作出材料性决定，也不能借执行便利扩大授权。
- 证据先于叙述。判断必须绑定固定事实；事实漂移时重新取证，不沿用过期结论。
- 单一事实源与单 writer。每项状态只有一个权威来源和一个写入者，避免并发叙述互相覆盖。
- 静默编排。健康等待不制造进度噪声，只在完成、异常、需要决定或证据改变时返回。
- 保护本机。资源受限环境不承担大型依赖、构建产物或缓存的峰值成本，事后清理不能替代事前边界。
- CI 策略依据实时事实。速度与成本取舍属于目标项目 policy，通用机制不把单个项目选择写成全局规则。
- 流程成本保持最少必要。增加步骤必须提高控制、恢复、验证或安全中的至少一项，而不是只增加仪式。
- GitHub 通用机制与项目 policy 分层。可移植机制保持通用，仓库特有门禁留在项目事实源。
- 数据保护聚焦防泄漏、最小留存和边界验证；GKD 不扩张为通用 Cyber 平台。
- 长任务把恢复能力作为正常路径，发行过程把自验证作为产品能力，而不是交付后的补充检查。

## 冲突时的决策顺序

发生冲突时，依次服从：用户已批准意图与数据保护；正确性和固定证据；可恢复与可移植；本机资源边界；目标项目速度与成本 policy；流程与上下文简洁。后项不得覆盖前项。

## 明确非目标

- 不替代用户作出材料性产品、自动化或外部动作决定。
- 不把 GKD 建成通用安全运营、威胁检测或 Cyber 平台。
- 不把某个仓库的 policy、资源条件或发布习惯固化成所有项目的机制。
- 不要求消费项目的普通改动承担 GKD 发行物的完整自验证成本。
- 不用更多流程、更多上下文或更多并发本身衡量成功。

## 演进规则

愿景只在长期使命、承诺或取舍顺序发生材料性变化时更新，并须先获得明确决定。具体实现、短期任务、操作步骤和易变事实留在各自权威层。演进应保留向后可理解的理由、可复核证据与恢复路径；任何新增机制都要说明它如何改善用户控制、正确性、恢复、移植、资源保护或最少必要流程。
